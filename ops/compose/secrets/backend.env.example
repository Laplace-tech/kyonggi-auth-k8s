# docker-compose 가 읽어서 컨테이너 환경변수로 주입한다.

# DB
SPRING_DATASOURCE_PASSWORD=change-me

# JWT secret (권장: 32 bytes 이상 랜덤, hex/base64 가능)
# 예: openssl rand -hex 32  -> 64 hex chars (=32 bytes)
APP_AUTH_JWT_SECRET=change-me-please-generate-random-32bytes-min

# OTP HMAC secret (권장: JWT와 별도 키)
APP_OTP_HMAC_SECRET=change-me-please-generate-random-32bytes-min
